apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-#n-deployment
  labels:
    app: microservice-#n
spec:
  replicas: 1
  selector:
    matchLabels:
      app: microservice-#n
  template:
    metadata:
      labels:
        app: microservice-#n
    spec:
      containers:
      - name: microservice
        image: gcr.io/PROJECT_ID/BASE_IMAGE_n#n
        ports:
        - containerPort: 8080
